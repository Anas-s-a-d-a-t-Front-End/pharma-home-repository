import 'package:flutter/material.dart';

class complete_the_order extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('أضافة بطاقة الائتمان'),
          centerTitle: true,
        ),
        body: Scaffold(
          body: Container(
            width: double.infinity,
            child: ListView(
              children: [],
            ),
          ),
        ));
  }
}
